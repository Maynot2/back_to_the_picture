# Installation
1 - create a folder in your computer and copy-paste the content of the Vagrantfile in a new Vagrantfile
2 - vagrant up
3 - vagrant ssh
4 - git clone this repo
5 - execute ./install
6 - quit your vm and come back
7 - execute the file ./make_sure, to be sure everything was install
8 - You can start

# POSTGRESQL
	1 - sudo -u postgres psql -f setupdb.sql # Create db and tables
	2 - sudo -u postgres psql -f populate.sql # Add data in db

	# If you want to connect to postgres :
	1 - sudo -i -u postgres psql
	# Commands
	Connect to a database : \c <database_name>;
	Show tables in database : \dt;
	Quit postgres : \q;


[NOT NOW ] # For expressJS and ReactJS
npm init, at the time to install all dependencies with the package.json
